language: "typescript"
layer: "application"
stack: "frontend"

project:
  title: "docs-site"
  description: "Stemix documentation site built with Docusaurus 3."

tasks:
  all:
    command: ["bash", "-c", "true"]
    deps: ["install", "build", "check"]
  install:
    command: ["bash", "-c", "PUPPETEER_SKIP_DOWNLOAD=true npm ci"]
  build:
    command: ["npm", "run", "build"]
  clean:
    command: ["bash", "-c", "rm -rf build .docusaurus"]
  generate-diagrams:
    command: ["npm", "run", "generate:diagrams"]
  check-diagrams:
    command: ["npm", "run", "check:diagrams"]
  check-lint:
    command: ["npm", "run", "lint:md"]
  check-test:
    command: ["npm", "run", "typecheck"]
  check-ci:
    command: ["bash", "-c", "true"]
    deps: ["install", "build", "check-diagrams", "check-lint", "check-test"]
    options:
      runInCI: true
  check:
    command: ["bash", "-c", "true"]
    deps: ["check-diagrams", "check-lint", "check-test"]
  run-dev:
    command: ["npm", "run", "start"]
    options:
      persistent: true
      runInCI: false
  run-serve:
    command: ["npm", "run", "serve"]
    options:
      persistent: true
      runInCI: false
